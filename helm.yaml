applications:
  - name: "eva.api"

    technology: static

    ingress:
      enabled: true
      domain: "rest.cabi.org"
      basePath: "/eva"

    azureAd:
      - server: eva-api

    seq:
      -enabled: false